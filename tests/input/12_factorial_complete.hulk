// Programa completo: Factorial con entrada
function factorial(n) {
    if (n <= 1) 
        return 1;
    else 
        return n * factorial(n - 1);
}

print "Factorial Calculator";
print "===================";

for (var i = 0; i <= 10; i = i + 1) {
    print i @ "! = " @ factorial(i);
};