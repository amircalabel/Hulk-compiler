// Prueba de clases básicas
type Point(x, y) {
    x = x;
    y = y;
    
    getX() => self.x;
    getY() => self.y;
    
    distance() {
        return sqrt(self.x ^ 2 + self.y ^ 2);
    }
    
    translate(dx, dy) {
        self.x := self.x + dx;
        self.y := self.y + dy;
    }
}

let p = new Point(3, 4) in {
    print "Point: (" @ p.getX() @ ", " @ p.getY() @ ")";
    print "Distance: " @ p.distance();
    p.translate(1, 1);
    print "After translation: (" @ p.getX() @ ", " @ p.getY() @ ")";
};