// Prueba de variables con let
let x = 42 in print x;

let a = 10, b = 20 in print a + b;

let msg = "Hello" in {
    print msg;
    let inner = " World" in print msg @ inner;
};

// Asignación destructiva
let counter = 0 in {
    print counter;
    counter := counter + 1;
    print counter;
};