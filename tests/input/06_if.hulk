// Prueba de if/elif/else
let x = 5 in {
    if (x > 0) 
        print "positive";
    else 
        print "non-positive";
};

let y = 0 in {
    if (y > 0)
        print "positive";
    elif (y < 0)
        print "negative";
    else
        print "zero";
};
