type Animal {
    speak(): String => "";
}

type Dog inherits Animal {
    speak(): String => "Woof!";
}

type Cat inherits Animal {
    speak(): String => "Meow!";
}

let animals = [new Dog(), new Cat()] in {
    for (animal in animals) {
        print animal.speak();
    }
}